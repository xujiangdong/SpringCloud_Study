server:
  port: 8007

spring:
  application:
    name: config-client1
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      profile: tst
      name: config-tst

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
